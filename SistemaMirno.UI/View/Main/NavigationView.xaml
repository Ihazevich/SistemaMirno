<UserControl x:Class="SistemaMirno.UI.View.Main.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SistemaMirno.UI.View"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ListBox Name="NavigationListBox"  ItemsSource="{Binding WorkAreas}"
             SelectedItem="{Binding SelectedWorkArea, Mode=TwoWay}" 
             IsEnabled="{Binding NavigationEnabled}">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Button Content="{Binding Name}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Command="{Binding DataContext.ChangeViewCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}"
                        CommandParameter="{Binding Id}"
                        FontSize="20"
                        Margin="5"/>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>