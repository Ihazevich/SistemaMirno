<mah:MetroWindow x:Class="SistemaMirno.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SistemaMirno.UI"
        xmlns:view="clr-namespace:SistemaMirno.UI.View"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Height="450"
        Width="800"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <Menu DockPanel.Dock="Top" Style="{StaticResource MahApps.Styles.Menu}">
            <MenuItem Header="Datos">
                <MenuItem Header="Materiales" Command="{Binding ChangeViewCommand}" CommandParameter="Materials" />
                <MenuItem Header="Colores" Command="{Binding ChangeViewCommand}" CommandParameter="Colors" />
                <MenuItem Header="Productos" Command="{Binding ChangeViewCommand}" CommandParameter="Products" />
                <MenuItem Header="Categorias de Producto" Command="{Binding ChangeViewCommand}" CommandParameter="ProductCategories" />
                <MenuItem Header="Areas de Produccion" Command="{Binding ChangeViewCommand}" CommandParameter="ProductionAreas" />
                <MenuItem Header="Recursos Humanos">
                    <MenuItem Header="Empleados" Command="{Binding ChangeViewCommand}" CommandParameter="Employees" />
                    <MenuItem Header="Roles" Command="{Binding ChangeViewCommand}" CommandParameter="EmployeeRoles" />
                </MenuItem>
            </MenuItem>
        </Menu>
        <view:WorkAreaNavigationView DockPanel.Dock="Left" DataContext="{Binding WorkAreaNavigationViewModel}" />
        <ContentPresenter DockPanel.Dock="Right" Content="{Binding SelectedViewModel}" />
    </DockPanel>
</mah:MetroWindow>